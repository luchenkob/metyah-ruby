var eventsModal = $('#modal');

if (eventsModal.length > 0) {
  eventsModal.html("<%= j render(partial: 'user/events/modal') %>");
} else {
  console.log('#modal not found');
}
eventsModal.modal({
  show: true
});